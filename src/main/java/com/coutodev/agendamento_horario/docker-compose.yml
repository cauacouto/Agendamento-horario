services:
  backend:
    build: ./backend
    container_name: agendamento-Api
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker

  frontend:
    build: ./frontend
    container_name: FRONT
    ports:
      - "5173:5173"
    depends_on:
      - backend
